# Rename this file to "secrets.toml" (remove ".example") and set your own
# admin username/password. This file must stay inside a folder named
# ".streamlit" right next to app.py.
#
# IMPORTANT: never share this file or commit it to a public place (like a
# public GitHub repo) - it contains your login password in plain text.

ADMIN_USERNAME = "admin"
ADMIN_PASSWORD = "change-this-password"
